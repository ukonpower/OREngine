{
  "name": "demo-project",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev:server\" \"npm run dev:vite\"",
    "dev:server": "tsx watch scripts/server.mjs",
    "dev:vite": "vite",
    "build": "vite build --config vite-player.config.ts",
    "build:static": "vite build --config vite-static.config.ts"
  },
  "dependencies": {
    "orengine": "file:.."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tsx": "^4.19.0",
    "vite": "^5.4.10"
  }
}
